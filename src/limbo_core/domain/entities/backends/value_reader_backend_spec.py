"""Value reader backend binding spec."""

from __future__ import annotations

from .backend_spec import BackendSpec


class ValueReaderBackendSpec(BackendSpec):
    """Named value reader binding declared in project payload."""
