"""Path backend binding spec."""

from __future__ import annotations

from .backend_spec import BackendSpec


class PathBackendSpec(BackendSpec):
    """Named path backend binding declared in project payload."""
